# Iter-3 production dependencies.
# Pinned to the exact versions used during the 20260507_135733_three_way_promoted run.
# CPU-only install: drop "+cu121" from torch and skip the --extra-index-url.
# CUDA 12.1 install: pip install --extra-index-url https://download.pytorch.org/whl/cu121 -r requirements.txt

# Web / API
fastapi==0.124.2
uvicorn[standard]==0.38.0
pydantic==2.12.5
httpx==0.28.1

# Numeric / data
pandas==2.3.3
numpy==2.2.6
scipy==1.15.3
pyarrow==22.0.0
PyYAML==6.0.3

# ML
scikit-learn==1.7.2
sentence-transformers==5.1.2
torch==2.5.1+cu121
transformers==4.57.3
tokenizers==0.22.1
faiss-cpu==1.13.0
rank-bm25==0.2.2

# Capacity planning / benchmark tooling
psutil==7.1.3
matplotlib==3.10.8

# Test
pytest==9.0.3
