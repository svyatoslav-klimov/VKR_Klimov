[pytest]
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: tests that require fully loaded model artifacts
    smoke: minimal smoke checks runnable without external resources
